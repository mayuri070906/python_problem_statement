class Marks:
	def __init__(self):
		self.n=int(input("Enter a number:")
		self.count=0
		self.markList=[]
		for i in range(self.n):
			self.mark=int(input("Enter a number:")
			self.markList.append(mark)		
	def marks(self):
		for i  in self.markList:
			if self.markList[i]<=100 and self.markList[i]>=90:
				self.count+=1
			print(f"Total frist class students:{count}")
			elif self.markList[i]<90 and iself.markList[i]>=80:
				self.count+=1
				print(f"Total second class students:{count}")
			if self.markList[i]>35:
				self.count+=1
				print(f"Total pass students:{count}")
if __name__="__main__":
	obj=Marks()
	obj.marks()


			


		
		